[project]
name = "p07-shopping-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents>=1.0.0",
    "strands-agents[otel]",
    "bedrock-agentcore>=1.0.0",
    "boto3>=1.35.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
